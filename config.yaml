listen: :PORT_PLACEHOLDER

acme:
  domains:
    - yourdomain.com
  email: your@email.com

auth:
  type: password
  password: "dalong-strong-password"

# 启用 UDP-over-TCP fallback（Fly.io 免费套餐必须）
udp:
  enabled: true
  fallback_tcp: true

bandwidth:
  up: 100 mbps
  down: 100 mbps
